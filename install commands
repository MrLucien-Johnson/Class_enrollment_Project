sudo apt-get update
sudo apt install python3-pip
my keypare is called secret key not my secret key for database
pip install venv
py -m venv venv
source venv/scripts/activate

pip install Flask-WTF
pip install WTForms
pip install -U Flask
pip install flask_sqlalchemy
pip install flask-Testing
pip install -r requirements.txt

pull project 

#export database uri
#export secret key
pytest

py create.py
py app.py

load to webserver

can all be done in jenkins